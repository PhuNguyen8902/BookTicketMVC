import { Stack } from "@mui/material";
import styled from "styled-components";



export const ProfileAcountContainer = styled(Stack)(({
    width: "100%",
    margin: "100px 0",
    alignItems: "center",

}))

export const ProfileAcountContent = styled(Stack)(({
    width: "60%",
}))

//Profile
export const ProfileContainer = styled(Stack)(({
    width: "100%",
    alignItems: "center",
}))

export const ProfileContent = styled(Stack)(({
    width: "80%",
}))

//Acount
export const AccountContainer = styled(Stack)(({
    width: "100%",
    alignItems: "center",
}))

export const AccountContent = styled(Stack)(({
    width: "80%",
}))